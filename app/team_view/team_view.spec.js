'use strict';

describe('myApp.team_view module', function () {

  beforeEach(module('myApp.team_view'));

  describe('team_view controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var viewteam = $controller('TeamViewCtrl');
      expect(viewteam).toBeDefined();
    }));

  });
});
